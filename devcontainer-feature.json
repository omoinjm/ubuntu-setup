{
  "name": "Ubuntu Setup",
  "id": "ubuntu-setup",
  "version": "1.1.0",
  "description": "Personal Ubuntu development environment setup with Fish shell, Neovim, NVM, fzf, and optional Zsh/Terraform/Nebius CLI",
  "options": {
    "dotfilesRepo": {
      "type": "string",
      "default": "",
      "description": "Git repository URL for dotfiles (optional)"
    },
    "installFish": {
      "type": "boolean",
      "default": true,
      "description": "Install and configure Fish shell"
    },
    "installZsh": {
      "type": "boolean",
      "default": false,
      "description": "Install and configure Zsh shell (oh-my-posh prompt)"
    },
    "installNeovim": {
      "type": "boolean",
      "default": true,
      "description": "Install Neovim"
    },
    "installNodejs": {
      "type": "boolean",
      "default": true,
      "description": "Install Node.js"
    },
    "installTerraform": {
      "type": "boolean",
      "default": false,
      "description": "Install Terraform"
    },
    "installTmux": {
      "type": "boolean",
      "default": true,
      "description": "Install and configure tmux"
    },
    "installNebius": {
      "type": "boolean",
      "default": false,
      "description": "Install Nebius CLI"
    }
  },
  "installsAfter": ["ghcr.io/devcontainers/features/common-utils"]
}
